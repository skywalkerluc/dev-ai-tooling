## Stack: Scala

Detection markers: build.sbt, .scala-version

- Use sbt as default build entry when build.sbt exists
- Use project-defined scripts for install, test, lint, and typecheck when present.
