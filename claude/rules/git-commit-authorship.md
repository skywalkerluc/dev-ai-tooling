# Git commit authorship

When creating git commits:

- Never add `Co-authored-by` trailers for AI agents or automated assistants.
- The commit author must be only the human developer.
- Do not commit unless explicitly requested.
